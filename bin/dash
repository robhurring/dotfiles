#!/usr/bin/env sh

# Dash
# dash [language] <query>

dash() {
  local language_or_query="$1"
  local query="$2"

  if [ $# -eq 2 ]; then
    open "dash://${language_or_query}:${query}"
  else
    open "dash://${language_or_query}"
  fi
}

dash "$@"
